function displayError(errorId) {
    var errorMessage = document.getElementById(errorId);
    errorMessage.style.display = "block";
}

/* 
   Call this function with the appropriate error message ID 
   when needing to display an error message on the login page.
*/